apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: variable-substitution
  namespace: default
spec:
  interval: 1m
  path: "./my-configs"
  postBuild:
    substituteFrom:
      - kind: ConfigMap
        name: global-vars
        optional: true
