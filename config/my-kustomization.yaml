apiVersion: kustomize.toolkit.fluxcd.io/v1beta1
kind: Kustomization
metadata:
  name: variable-substitution
  namespace: default
spec:
  interval: 1m
  path: "./"
  postBuild:
    substituteFrom:
      - kind: ConfigMap
        name: global-vars
        optional: true
