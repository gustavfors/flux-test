apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - my-configmap.yaml
spec:
  postBuild:
    substituteFrom:
      - kind: ConfigMap
        name: global-vars
        optional: false
